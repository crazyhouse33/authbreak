#include <limits.h>

//Theses are set by the cli parser according to given user options
char* DEFAULT_CHARSET;
unsigned short DEFAULT_LEN_MIN;
unsigned short DEFAULT_LEN_MAX;
char DEFAULT_SEPARATOR;

